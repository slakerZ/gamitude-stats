namespace StatsApi.Models
{
    public enum RANK_SET {ANIMAL};

    public enum STATS {STRENGTH,INTELLIGENCE,CREATIVITY,FLUENCY};

    public enum BREAK {TALK,TASK,LAZY}; // TODO contact architect (product owner)
    //public enum TIER {A,B,C,D,F,S};

}